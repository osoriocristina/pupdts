function alert_error(message){
	Swal.fire(
		'Opps!',
		message,
		'error'
	  );
}

function alert_success(message){
	 Swal.fire(
	  'Success!',
	  message,
	  'success'
	);
}

function alert_warning(message){
	 Swal.fire(
	  'Warning!',
	  message,
	  'warning'
	);
}